name: Build SELF TO MRS

on:
  workflow_dispatch:
  push:
    paths:
      - "rules/self-built.txt"

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - name: Build SELF TO MRS
        run: |
          docker run --rm \
            -v ${{ github.workspace }}/rules:/rules \
            metacubex/mihomo:latest \
            convert-ruleset \
            domain \
            text \
            /rules/self-built.txt \
            /rules/self-built.mrs

      - name: Commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add rules/self-built.mrs

          if ! git diff --cached --quiet; then
            git commit -m "Update self-built.mrs"
            git push
          fi
